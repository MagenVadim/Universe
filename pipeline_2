def varC = "echo "Hello, LANGUAGE, CCCCC!""
def varPython = "{ echo "Hello, LANGUAGE, Python!" }"
def varBash = "{ echo "Hello, LANGUAGE, Bash!" }"
        

pipeline {
    agent any
    parameters {
        choice(
            choices: ['all' , 'C', 'Python', 'Bash'],
            description: '',
            name: 'LANGUAGE')
    }

    stages {
        stage ('ALL Language') {
            when { expression { params.LANGUAGE == 'all' } }
            steps { ${varC} }
            steps ${varPython}
            steps ${varBash}
        }
        stage ('C') {
            when { expression { params.LANGUAGE == 'C' } }
            steps ${varC}
        }
        stage ('Python') {
            when { expression { params.LANGUAGE == 'Python' } }
            steps ${varPython}
        }
        stage ('Bash') {
            when { expression { params.LANGUAGE == 'Bash' } }
            steps ${varBash}
        }
    }
}
