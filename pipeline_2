pipeline {
    agent any
    parameters {
        choice(name: 'LANGUAGE', choices: ['all', 'C', 'Python', 'Bash'])
    }
    stages {
        stage('Example') {
            steps {
                echo "${LANGUAGE}, is priceeding..."
            }
        }
    }
}
